package com.jjava.lecture.ch05;

public class Reference {

	public static void main(String[] args) {

		/*
		 * 변수
		 * 1. 기본(원시)
		 * 2.참조(참조형이라는 뜻)
		 * - 배열 
		 * - 열거
		 * - 클래스(String)
		 * - 인터페이스
		 * 
		 * 
		 * 참조 : 냄비 안에 데이터가 있는 것이 아니라 데이터가 저장되어 있는 주소를 넣어 둔다.
		 * ㄴ 참조형도 초기화 : null 로
		 * ㄴ NullPointrException(npe) : 사용(대입 및 호출(접근))이 불가
		 * ㄴ ==. != 과 같은 관계 연산자는 사용 가능
		 * 
		 * String : 문자열 저장을 위한 객체
		 * String str1 = null; -> 클래스에 해당 
		 * String str2 = ""; -> 클래스에 해당
		 * String str3;
		 * 
		 * if(str1 == str2) { 주소값 비교
		 * }
		 * str1.equals(str2); 문자열 비교
		 * 
		 * */

	}

}
